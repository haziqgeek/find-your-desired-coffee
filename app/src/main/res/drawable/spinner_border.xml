<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Border -->
    <item>
        <shape android:shape="rectangle">
            <stroke android:color="#583931" /> <!-- Customize the border color as needed -->
            <corners android:radius="4dp" /> <!-- Customize the border corner radius as needed -->
        </shape>
    </item>

    <!-- Background color -->
    <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Customize the background color as needed -->
            <corners android:radius="4dp" /> <!-- Customize the background corner radius as needed -->
        </shape>
    </item>
</layer-list>